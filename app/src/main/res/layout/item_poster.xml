<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="320dp"
    android:layout_margin="8dp"
    android:clickable="true"
    android:focusable="true"
    app:cardElevation="4dp"
    app:cardCornerRadius="12dp">

    <!-- FrameLayout permite sobrepor elementos -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Conteúdo principal -->
        <LinearLayout
            android:orientation="vertical"
            android:padding="12dp"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/posterImage"
                android:layout_width="match_parent"
                android:layout_height="140dp"
                android:scaleType="centerCrop"
                android:background="#F5F5F5"/>

            <TextView
                android:id="@+id/posterTitle"
                android:textSize="15sp"
                android:textStyle="bold"
                android:textColor="#212121"
                android:maxLines="2"
                android:ellipsize="end"
                android:layout_marginTop="8dp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

            <TextView
                android:id="@+id/posterSubtitle"
                android:layout_marginTop="4dp"
                android:textSize="13sp"
                android:textColor="#757575"
                android:maxLines="1"
                android:ellipsize="end"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

            <TextView
                android:id="@+id/posterPrice"
                android:layout_marginTop="6dp"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#FF9800"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <!-- Spacer para empurrar os botões pro final -->
            <View
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"/>

            <LinearLayout
                android:layout_marginTop="8dp"
                android:orientation="horizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <Button
                    android:id="@+id/btnEdit"
                    android:layout_weight="1"
                    android:layout_width="0dp"
                    android:layout_height="38dp"
                    android:text="Editar"
                    android:textSize="13sp"
                    android:textColor="#424242"
                    android:background="@drawable/btn_edit_modern" />

                <Button
                    android:id="@+id/btnDelete"
                    android:layout_marginStart="8dp"
                    android:layout_weight="1"
                    android:layout_width="0dp"
                    android:layout_height="38dp"
                    android:text="Excluir"
                    android:textSize="13sp"
                    android:textColor="#FFFFFF"
                    android:background="@drawable/btn_delete_modern" />

            </LinearLayout>

        </LinearLayout>

        <!-- Flag de negociação sobreposta no canto superior direito -->
        <TextView
            android:id="@+id/flagNegociacao"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="EM NEGOCIAÇÃO"
            android:textSize="9sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:background="@drawable/flag_background"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:visibility="gone"
            android:layout_gravity="top|end"
            android:layout_margin="10dp"
            android:elevation="6dp"/>

    </FrameLayout>

</androidx.cardview.widget.CardView>